<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppOdontoPrev" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Cor principal do app -->
        <item name="colorPrimary">@color/blue_primary</item>
        <!-- Cor da status bar -->
        <item name="android:statusBarColor">@color/blue_primary</item>
        <!-- Outras cores do sistema -->
        <item name="colorPrimaryDark">@color/blue_primary</item>
        <item name="colorAccent">@color/blue_primary</item>
        <!-- Opcional: Para ícones escuros na status bar (remova se preferir ícones claros) -->
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.AppOdontoPrev" parent="Base.Theme.AppOdontoPrev" />

    <color name="blue_primary">#0066FF</color>
</resources>